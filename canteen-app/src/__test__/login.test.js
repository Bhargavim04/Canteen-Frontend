import Login from "../components/login";
import { render, screen, fireEvent } from "@testing-library/react";


describe("Login component test cases", () => {
  //define test cases
//   test("verify login component title is correct or not", () => {
//     // Render Counter component into virtual dom
//     const component = render(<Login />);
//     // Get title element by using testid
//     const titleEle = component.getByTestId("title");
//     // Compare title
//     expect(titleEle.textContent).toBe("Login Form");
//   }); //test-end

//   test("email input should be rendered", () => {
//     // Render Counter component into virtual dom
//     render(<Login />);
//     // Get title element by using testid
//     const userInputEl = screen.getByPlaceholderText(/EmailAddress/i);
//     // Compare title
//     expect(userInputEl).toBeInTheDocument();
//   }); //test-end
});